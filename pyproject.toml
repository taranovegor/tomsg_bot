[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]
markers = [
    "live: mark test as requiring live network access (excluded from default run)",
]
addopts = "--disable-socket --allow-unix-socket -m 'not live'"
